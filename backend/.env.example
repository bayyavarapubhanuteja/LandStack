# PostgreSQL + PostGIS (recommended). Falls back to SQLite for quick local demos if unset.
DATABASE_URL=postgresql+psycopg2://landstack:landstack@localhost:5432/landstack
# DATABASE_URL=sqlite:///./landstack.db
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_MINUTES=480
CORS_ORIGINS=http://localhost:5173
